# Backend

Organizational notes only. The actual Java source that compiles lives at
`src/main/java/com/ironhold/` (required by Maven — do not move it here).

Use this folder for backend design notes: class responsibilities, method
contracts, balance numbers, anything that isn't code but explains the code.
