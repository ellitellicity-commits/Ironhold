# Frontend

HTML/CSS/JS frontend, vibe-coded, embedded in the Java app via JavaFX's WebView.
Java pushes game state to this layer each tick via WebEngine.executeScript(...).
No build framework required — plain HTML/CSS/JS.
